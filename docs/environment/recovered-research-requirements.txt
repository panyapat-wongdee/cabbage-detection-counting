# Sanitized direct dependency snapshot recovered from the private research archive.
# This is provenance evidence, not a claim that every historical run used every
# listed package. The public install profiles are defined by pyproject.toml and
# requirements.txt.

albumentations==1.4.21
matplotlib==3.9.2
numpy==2.1.3
opencv-python==4.10.0.84
pycocotools==2.0.8
PyYAML==6.0.2
torch==2.5.1+cu118
torchvision==0.20.1+cu118
ultralytics==8.4.9
