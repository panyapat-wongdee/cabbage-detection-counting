# Direct research/runtime dependencies.
# Versions are taken from docs/environment/recovered-research-requirements.txt,
# the sanitized environment snapshot recovered from the private archive. This
# curated file does not
# duplicate every transitive package recorded in that snapshot.
--extra-index-url https://download.pytorch.org/whl/cu118

albumentations==1.4.21
numpy==2.1.3
opencv-python==4.10.0.84
pycocotools==2.0.8
# Repository runtime dependency for terminal progress reporting.
tqdm==4.67.1
PyYAML==6.0.2
matplotlib==3.9.2
torch==2.5.1+cu118
torchvision==0.20.1+cu118
ultralytics==8.4.9
